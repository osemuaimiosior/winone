docker build -t winone_node_heartbeat . && docker tag winone_node_heartbeat osemu/winone_node_heartbeat && docker push osemu/winone_node_heartbeat

Private registry: registry.yourdomain.com/winone_node_heartbeat

docker run -d \
    --restart unless-stopped \
    -e REGISTRY_SERVER_ADDRESS=52.90.249.116:50053 \
    -e QUEUE_SERVER_ADDRESS=52.90.249.116:50052 \
    -v $(pwd)/winone-data:/var/lib/winone \
    --name winone-heartbeat \
    osemu/winone_node_heartbeat

