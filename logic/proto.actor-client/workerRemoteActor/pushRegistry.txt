step 1. 

docker build -t winone-hpc-worker . && docker tag winone-hpc-worker osemu/winone-hpc-worker && docker push osemu/winone-hpc-worker

docker run \
     --network host \
     --restart unless-stopped \
    --name winone-hpc-worker \
    -e MASTER_PID_HOST=100.79.54.6:8090 \
    osemu/winone-hpc-worker

Private registry
registry.yourdomain.com/winone-hpc-worker